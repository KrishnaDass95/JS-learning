let a = 5 + 10 * 20 / 30 // Arithmetic operator
if(5 < 10 && 2 > 2 || 8 > 9 ){ // Logical operator  - When comparing && (both need to be true)
    // when comparing OR || any one needs to be true
    console.log(true)
}
else{
    console.log(false)
}

// Assignment operator: = += -=

// ternary operator:
let age = 200
let result = age > 10 ? "TRUE YO" : "Falseyp"
console.log(result);

// == and ===
console.log(3 == '3'); // only checks value
console.log(3 === "3") // checks value and type